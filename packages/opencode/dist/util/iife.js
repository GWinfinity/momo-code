/**
 * Immediately-invoked function expression utility.
 * Provides a type-safe way to execute a function immediately.
 */
/**
 * Execute a function immediately and return its result.
 * Useful for creating const bindings with complex initialization logic.
 */
export function iife(fn) {
    return fn();
}
//# sourceMappingURL=iife.js.map